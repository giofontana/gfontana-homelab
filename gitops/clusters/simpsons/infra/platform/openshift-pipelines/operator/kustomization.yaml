apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../../../../components/infra/openshift-pipelines

patches:
  - target:
      kind: Subscription
      name: openshift-pipelines-operator
    path: patch-version.yaml
